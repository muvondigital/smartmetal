# SmartMetal Docker Compose Environment Variables
# Copy this file to .env and fill in your actual values

# Azure OpenAI Configuration (optional for local dev)
AZURE_OPENAI_ENDPOINT=https://your-instance.openai.azure.com/
AZURE_OPENAI_KEY=your-azure-openai-key
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# Azure Document Intelligence Configuration (optional for local dev)
AZURE_DOC_INTELLIGENCE_ENDPOINT=https://your-instance.cognitiveservices.azure.com/
AZURE_DOC_INTELLIGENCE_KEY=your-azure-doc-intelligence-key

# SMTP Configuration (optional for email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-smtp-password

# Note: Database and Redis credentials are configured in docker-compose.yml
# For production, use managed Supabase instance instead of local PostgreSQL
