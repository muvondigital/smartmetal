// NOT IN USE. Legacy placeholder for future agreement API. Do not import without proper implementation.
//
// Price agreements routes are currently handled by priceAgreementsRoutes.js (note: plural).
// This file exists for potential future expansion but has no active functionality.
//
// If you need agreement routes, use: require('./priceAgreementsRoutes')

const express = require('express');
const router = express.Router();

// Empty router - not mounted in main app
module.exports = router;
