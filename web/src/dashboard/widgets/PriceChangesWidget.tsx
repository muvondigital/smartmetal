/**
 * Price Changes Widget
 *
 * Displays recent material price changes.
 * Part of SmartMetal Dashboard Framework.
 * Wraps the existing PriceChangeNotifications component.
 */

import { PriceChangeNotifications } from '../../components/dashboard/PriceChangeNotifications';

export function PriceChangesWidget() {
  return <PriceChangeNotifications />;
}
